# FacePanel - Система распознавания лиц и контроля посещений

## 📋 Описание проекта

**FacePanel** - это комплексная система для распознавания лиц и контроля посещений на мероприятиях. Система состоит из трех основных компонентов:

- **Spring Boot Backend** - серверная часть для обработки данных распознавания
- **Python OpenCV Script** - клиентская часть для захвата видео и распознавания лиц
- **Микроконтроллеры ESP01 + реле. Для открытие турникета по http запросу

## 🏗️ Архитектура системы

### Backend (Spring Boot)
- **Framework**: Spring Boot 3.x
- **Database**: PostgreSQL
- **ORM**: JPA/Hibernate
- **Template Engine**: Thymeleaf
- **Real-time Communication**: WebSocket + STOMP
- **Build Tool**: Maven

### Frontend
- **Templates**: Thymeleaf HTML
- **Styling**: Custom CSS (Dark Theme)
- **Real-time Updates**: JavaScript + SockJS + STOMP
- **Responsive Design**: Grid Layout

### Python Script
- **Computer Vision**: OpenCV (cv2)
- **Face Recognition**: Custom face detection algorithms
- **HTTP Client**: requests library
- **Image Processing**: PIL/Pillow

## 🎯 Основной функционал

### 1. Распознавание лиц в реальном времени
- Захват видео с камеры
- Детекция и распознавание лиц
- Отправка данных на сервер через REST API
- WebSocket уведомления для обновления интерфейса
- Поддержка нескольких камер: в payload передаётся `cameraName` (например, `KPP1`, `KPP2`) для фиксации источника распознавания

### Многокамерный Python‑клиент
- Запуск двух процессов на одной машине: для каждой камеры свой процесс.
- Основной скрипт `OpenCVRework/face.py` принимает параметры `--camera-url` и `--camera-name`.
- Для удобства добавлены обёртки `face_kpp1.py` и `face_kpp2.py` — пропишите RTSP/HTTP URL и запускайте параллельно.

### 2. Управление мероприятиями
- Создание и завершение мероприятий
- Автоматическое создание "Default" сессии при первом распознавании
- Контроль посещений в реальном времени
- Статистика участников

### 3. Управление персонами
- CRUD операции для персон
- Связь с файлами изображений лиц
- Автоматическое обновление списков через WebSocket

### 4. История и аналитика
- Детальная история мероприятий
- Время прихода каждого участника
- Статистика посещаемости
- Фильтрация по датам

## 🗄️ Структура базы данных

### Основные сущности:
- **Person** - информация о персонах (имя, фамилия, должность, файл фото)
- **Session** - мероприятия/сессии (название, время начала/окончания, статус)
- **Attendance** - записи о посещениях (персона, сессия, время, тип события)

### Связи:
- Person ↔ Attendance (One-to-Many)
- Session ↔ Attendance (One-to-Many)

## 🔄 Поток данных

1. **Python скрипт** захватывает видео и распознает лица
2. **REST API** получает данные распознавания
3. **AttendanceService** создает записи о посещениях
4. **WebSocket** отправляет уведомления клиентам
5. **Frontend** обновляется в реальном времени

## 📱 Интерфейсы

### Dashboard (`/`)
- Логи распознаваний в реальном времени
- WebSocket подключение с индикатором статуса
- Автоматическое обновление без перезагрузки

### Мероприятия (`/session`)
- Создание и управление мероприятиями
- Контроль посещений в реальном времени
- Статистика участников
- История мероприятий с детальной информацией

### Персоны (`/persons`)
- Управление базой персон
- CRUD операции
- Связь с файлами изображений

### История (`/attendance`)
- Полная история посещений
- Фильтрация по датам
- Детальная информация о каждом событии

## 🔧 Технические особенности

### WebSocket Integration
- Автоматическое переподключение при потере соединения
- Уведомления о новых распознаваниях
- Обновления списков персон в реальном времени
- Индикаторы статуса подключения

### Real-time Updates
- Новые логи появляются сверху списка
- Автоматическое обновление статистики
- Анимации при изменениях
- Без необходимости перезагрузки страницы

### Data Consistency
- Автоматическое создание Default сессий
- Связь персон с файлами изображений
- Валидация данных на всех уровнях
- Обработка ошибок и исключений

## 🎨 UI/UX Features

### Dark Theme
- Современный темный дизайн
- Контрастные цвета для лучшей читаемости
- Адаптивная верстка для всех устройств

### Real-time Indicators
- Статус WebSocket подключения
- Анимации при обновлениях
- Цветовая индикация статусов

### User Experience
- Интуитивная навигация
- Подтверждения для критических действий
- Подсказки и валидация форм
- Быстрый доступ к основным функциям

## 📊 Мониторинг и логирование

- Детальное логирование всех операций
- Отслеживание WebSocket соединений
- Мониторинг производительности распознавания
- Статистика использования системы
- Отправка отчетов явки.
---

## 🚀 Быстрый запуск через Лаунчер

Для удобного управления всеми компонентами системы создан **графический лаунчер**.

### Запуск лаунчера:
1. **Двойной клик на `launcher.bat`** (Windows)
2. Или запустите `python launcher.py` из корня проекта

### Возможности лаунчера:
- ✅ **Запуск/остановка Java Web приложения** (панель)
- ✅ **Запуск/остановка Python скрипта KPP1**
- ✅ **Запуск/остановка Python скрипта KPP2**
- ✅ **Редактирование IP адресов камер** для KPP1 и KPP2
- ✅ **Мониторинг логов** всех процессов в реальном времени
- ✅ **Автоматическое использование venv** для Python скриптов

### Требования для лаунчера:
- Python 3.x (должен быть в PATH)
- Для Java приложения: Maven (в PATH) или собранный JAR файл в `target/`
  - Чтобы собрать JAR: в Idea выполните `Maven -> Lifecycle -> package`
  - Или через командную строку: `mvn package`

### Примечание:
Лаунчер автоматически использует Python из `OpenCVRework/venv`, поэтому не нужно вручную активировать venv!

---

## ⚠️ Важное примечание
**Вход в оболочку venv\Scripts\Activate**

**Если запускать через `py face.py`, то будет ошибка на то что нету cv2. Нужно запускать вот так `python face.py`. (Обязательно с активным venv)**

**Запуск теста с вебки, а не с ip камеры:** 
   
   **python face.py --camera-index 0 --camera-name KPP1**


**TEST CAMERA 192.168.1.10**
